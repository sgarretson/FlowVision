{
  "name": "flowvision-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --env-file=.env --enable-source-maps dist/server.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@octokit/rest": "^20.1.1",
    "@octokit/webhooks": "^12.0.11",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "tsx": "^4.16.2",
    "typescript": "^5.9.2"
  }
}
